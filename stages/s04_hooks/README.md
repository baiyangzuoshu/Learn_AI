# 04: Hooks — 扩展点挂在循环上

[← s03](../s03_permission/README.md) · [课程地图](../README.md) ·
[s05 →](../s05_todo_write/README.md)

> “围绕循环加 Hook，不重写循环”
>
> Harness 层：生命周期。本章只增加一个机制，Agent 的核心决策循环保持不变。

## 本章目标

学完后，你应该能解释这个机制解决的具体问题、指出它插入 Agent Loop
的位置，并能修改一个约束后验证行为变化。

## 问题

日志、审批、指标和通知若直接塞进主循环，会把决策路径变成不可维护的第二工作流。

## 解决方案

在稳定生命周期点发布 UserPromptSubmit、PreToolUse、PostToolUse、ErrorRecovery 和 Stop。

## 工作原理

1. 注册独立 Hook。
2. 在工具前允许拦截。
3. 在工具后记录有界元数据。
4. 在恢复与停止时发布诊断事件。

### 执行链

```text
prompt → pre-tool → execute → post-tool → stop
```

模型负责决定下一步，Harness 负责校验、执行、记录和限制。失败也作为观察返回模型，而不是被隐藏。

## 读代码

- 实现：[code.ts](./code.ts)
- 运行：`deno task s04`
- 类型检查：`deno check stages/s04_hooks/code.ts`

沿着注册点、输入校验、执行函数和 `agentLoop()`
四处阅读。先找到本章新增的状态或工具，再追踪它如何复用前一章。

## 观察清单

记录一次完整任务的事件顺序，并观察同步 Hook 对关键路径延迟的影响。

建议同时记录：模型看见了什么 Schema、Harness
实际执行了什么、结果如何回到消息历史，以及取消或失败发生在哪一层。

## 边界与生产差距

每类 Hook 要明确 fail-open 或 fail-closed；事件必须脱敏，开发者事件默认不应进入普通用户界面。

课程代码为了突出机制会使用简化存储、协议或策略。不要把它直接导入 `src/`；迁移时应围绕生产
Registry、Runtime 和 Feature 契约重新实现。

## 动手练习

1. 修改一个预算、状态或校验条件，预测行为后再运行验证。
2. 构造一个失败输入，确认错误有界、可观察，且不会破坏已完成状态。
3. 写出该机制迁移到生产 `HarnessFeature` 时需要的输入、事件、权限类别和取消路径。

## 过关标准

- 能不看代码画出上面的执行链。
- 能运行本章并解释一次关键事件或工具结果。
- 能说清教学简化与生产边界，而不是只描述“功能能用”。

## 下一章

进入 [s05 →](../s05_todo_write/README.md)，在同一个 Loop 上继续增加下一项能力。
